# Baked into the SPA bundle at build time (`npm run build`, run by the
# Static Web Apps CI). Consumed by src/api/http.ts (VITE_API_BASE_URL ?? '/api').
#
# Replace the host below with your deployed Container App URL once it exists
# (deploy/deploy-azure.ps1 prints it). Keep the trailing /api so requests match
# the controller routes the dev Vite proxy used.
VITE_API_BASE_URL=https://wms-api.greenglacier-ac4f98a6.northeurope.azurecontainerapps.io/api
